<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangzhe.blog.mapper.AppImageRelationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wangzhe.blog.entity.AppImageRelation">
        <id column="id" property="id"/>
        <result column="relation_id" property="relationId"/>
        <result column="image_url" property="imageUrl"/>
        <result column="apply_type" property="applyType"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectImageByRelationId" resultType="string">
        select image_url
        from app_image_relation
        where relation_id = #{relationId}
          and apply_type = #{applyType}

    </select>

</mapper>
